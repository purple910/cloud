spring:
  application:
    name: nacos-gateway

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848

zuul:
  routes:
    auth:
      path: /auth/**
      serviceId: nacos-consumer
    pay:
      path: /pay/**
      serviceId: nacos-provider


